STOP. Do not check the frontend. Do not confirm 
types or hook mappings. The frontend is correct.

The ONLY problem is in the backend SQL query.
Only modify:
backend/src/Casrr.Infrastructure/SqlServer/
SqlReviewRepository.cs

The column Sample_type exists in 
dbo.[02_CORE_02_Reviews] and has data, but it 
is NOT in the SELECT list of GetReviewHeaderByIdAsync.

Do exactly these steps:

STEP 1: Show me the LAST 5 lines of the SELECT 
list in GetReviewHeaderByIdAsync right now.

STEP 2: Add these two columns to the END of 
the SELECT list in ALL THREE queries:
  r.[Sample_type],
  r.[Sample_criteria]

STEP 3: Add to ReviewHeader class:
  public string? SampleType { get; init; }
  public string? SampleCriteria { get; init; }

STEP 4: In the SqlDataReader mapping, add 
at the end (use the next available ordinal numbers):
  SampleType = rdr.IsDBNull(N) ? null : rdr.GetString(N),
  SampleCriteria = rdr.IsDBNull(N+1) ? null : rdr.GetString(N+1)

STEP 5: In GetReviewByEcifAsync and 
GetReviewByKeysAsync where ReviewInfoSection 
is constructed, add:
  SampleType = row?.SampleType,
  SampleTarget = row?.SampleCriteria,

STEP 6: Check if ReviewInfoSection in 
Casrr.Domain/ReviewQueue.cs has SampleType 
and SampleTarget properties. If not, add them.

Show the current last 5 lines of SELECT first, 
then show exact before/after for every change.
Do NOT touch any frontend file.