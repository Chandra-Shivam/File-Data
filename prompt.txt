Read AGENTS.md and UI-PATTERNS.md first.
Frontend only. No backend changes.

FILE 1: frontend/src/services/api/reviews.ts
1. Remove view parameter from getReviewQueuePage
2. Add to ReviewQueuePageResponse type:
   currentUserName?: string
   teamMemberNames?: string[]

FILE 2: frontend/src/app/review-queue/page.tsx
1. Remove view from getReviewQueuePage call
2. Remove myView from useEffect dependencies
3. After loading, store from response:
   const [currentUserName, setCurrentUserName] = 
     useState<string>("")
   const [teamMemberNames, setTeamMemberNames] = 
     useState<string[]>([])
   
   On load:
   setCurrentUserName(res.currentUserName ?? "")
   setTeamMemberNames(res.teamMemberNames ?? [])

4. Filter rows client-side based on myView.
   Add this after rows state:

   const filteredRows = rows.filter(row => {
     if (myView === 'dept') return true
     if (myView === 'my') 
       return row.reviewer === currentUserName
     if (myView === 'team') 
       return teamMemberNames.includes(row.reviewer)
     return true
   })

5. Replace rows with filteredRows in the 
   DataTable component.

Show exact before/after. No backend changes.