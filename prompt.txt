Read AGENTS.md and UI-PATTERNS.md first.
Frontend only. No backend changes.

FILE 1: frontend/src/services/api/reviews.ts

1. Remove view parameter from getReviewQueuePage — 
   revert to only accepting sampleId:
   export async function getReviewQueuePage(
     sampleId?: number): Promise<ReviewQueuePageResponse>

2. Add to ReviewQueuePageResponse type:
   currentUserName?: string
   teamMemberNames?: string[]

Show exact before/after.

FILE 2: frontend/src/app/review-queue/page.tsx

1. Remove view from getReviewQueuePage call:
   BEFORE: getReviewQueuePage(sampleIdNum, myView)
   AFTER:  getReviewQueuePage(sampleIdNum)

2. Remove myView from useEffect dependency array

3. After the existing rows state, add:
   const [currentUserName, setCurrentUserName] = 
     useState<string>("")
   const [teamMemberNames, setTeamMemberNames] = 
     useState<string[]>([])

4. Inside the loadQueue useEffect, after setting 
   rows from the response, add:
   setCurrentUserName(res.currentUserName ?? "")
   setTeamMemberNames(res.teamMemberNames ?? [])

5. After rows state, compute filteredRows:
   const filteredRows = useMemo(() => {
     if (myView === 'dept') return rows
     if (myView === 'my')
       return rows.filter(r => 
         r.reviewer === currentUserName)
     if (myView === 'team')
       return rows.filter(r => 
         teamMemberNames.includes(r.reviewer))
     return rows
   }, [rows, myView, currentUserName, teamMemberNames])

6. Replace rows with filteredRows in the 
   DataTable component:
   BEFORE: rows={sortedRows}
   Note: sortedRows is likely derived from rows.
   Update the sort source to use filteredRows:
   
   Find where sortedRows is computed — it likely 
   sorts the rows array. Change it to sort 
   filteredRows instead:
   BEFORE: [...rows].sort(...)
   AFTER:  [...filteredRows].sort(...)

Show exact before/after for every change.
No backend changes.