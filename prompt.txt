Read AGENTS.md and UI-PATTERNS.md first.
Backend changes only. No frontend files.

STEP 1 — Revert the SQL in SqlReviewRepository.cs
In GetQueueRowsAsync, remove the CTE and view 
filter. Revert WHERE clause to:
WHERE (r.[Cancelled] IS NULL OR r.[Cancelled] = 0)
  AND (@sampleId IS NULL OR r.[Sample_id] = @sampleId)

Remove @view and @empNumber SQL parameters from 
the query and SqlCommand.

STEP 2 — Add current user + team lookup
In GetQueuePageAsync, after fetching rows, run 
this additional query using the empNumber parameter:

SELECT 
  cu.[CAS_user_name] AS CurrentUserName,
  tm.[CAS_user_name] AS TeamMemberName
FROM dbo.[03_LIBRARY_05_CAS_Users] cu WITH (NOLOCK)
LEFT JOIN dbo.[03_LIBRARY_05_CAS_Users] tm 
  WITH (NOLOCK)
  ON tm.[CAS_user_manager] = cu.[CAS_user_name]
WHERE cu.[CAS_user_id] = @empNumber

Read results into:
- currentUserName: string (first row's CurrentUserName)
- teamMemberNames: List<string> (all non-null 
  TeamMemberName values)

STEP 3 — Add to ReviewQueuePageResponse
Find ReviewQueuePageResponse in Casrr.Domain.
Add two new properties:
  public string CurrentUserName { get; init; } = ""
  public IReadOnlyList<string> TeamMemberNames 
    { get; init; } = Array.Empty<string>()

STEP 4 — Remove view parameter from the chain
Remove view parameter from:
- GetQueueRowsAsync
- GetQueuePageAsync  
- IReviewRepository.GetQueuePageAsync
- IReviewService.GetReviewQueuePageAsync
- ReviewService.GetReviewQueuePageAsync
- ReviewController — remove [FromQuery] string? view
  and revert to: 
  _svc.GetReviewQueuePageAsync(sampleId, emp, ct)

Keep empNumber in GetQueuePageAsync — it is 
needed for the user lookup query.

Show exact before/after for every file changed.
No frontend changes.