STOP. Only modify the date inputs in the 
Reconsideration and Appeal modals in:
frontend/src/app/review/[ecif]/review-info/components/
sections/ReviewInfoSection.tsx

THE PROBLEM IS NOT REACT STATE — IT IS CSS.
globals.css has this rule:
  input[type="date"]:not(.has-value) { color: #0000 }
This makes the date value invisible when the 
.has-value class is missing.

Fix: Dynamically add the has-value class to both 
date inputs when they have a non-empty value.

RECONSIDERATION date input:
BEFORE:
  className="w-full h-9 border border-slate-300 
    rounded px-2 text-sm bg-white"

AFTER:
  className={`w-full h-9 border border-slate-300 
    rounded px-2 text-sm bg-white
    ${rcDate ? 'has-value' : ''}`}

APPEAL date input:
BEFORE:
  className="w-full h-9 border border-slate-300 
    rounded px-2 text-sm bg-white"

AFTER:
  className={`w-full h-9 border border-slate-300 
    rounded px-2 text-sm bg-white
    ${apDate ? 'has-value' : ''}`}

Also revert both date inputs back to controlled 
(value= not defaultValue=) since state IS updating 
correctly — the issue was CSS not React:

  value={rcDate}    (not defaultValue)
  value={apDate}    (not defaultValue)

No other changes. Show exact before/after.