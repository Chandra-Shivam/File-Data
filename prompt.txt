Read AGENTS.md and UI-PATTERNS.md first.

TWO TASKS in these files only:
- frontend/src/app/review/[ecif]/review-info/
  components/sections/ReviewInfoSection.tsx
- frontend/src/app/review/[ecif]/review-info/
  components/sections/hooks/useReviewInfo.ts
- frontend/src/services/api/reviews.ts

TASK 1 — Sample Type and Sample Target not showing

Before making changes, read and report:
1. In useReviewInfo.ts — what variable holds 
   the ReviewInfo details section (d)? 
   Show the current mapping for sampleType 
   and sampleTarget fields.
2. In ReviewInfoSection.tsx — show the exact 
   JSX for SAMPLE TYPE and SAMPLE TARGET fields.
3. In reviews.ts — does ReviewInfoSection type 
   have sampleType and sampleTarget fields?

Then fix any missing mappings:
- The values come from review.sampleType and 
  review.sampleTarget in the API response
- If missing from the type: add them as 
  sampleType?: string | null and 
  sampleTarget?: string | null
- If missing from hook mapping: map them from 
  the review object
- If missing from JSX: bind them to 
  d?.sampleType and d?.sampleTarget

Show exact before/after for each fix.

TASK 2 — Unlock Review modal changes

Find the unlock modal JSX (unlockOpen && ...) 
and make these 3 changes:

CHANGE 1 — Remove first radio option
The modal has 3 options:
1. Unlock review for general revisions (GENERAL)
2. Unlock review for reconsideration (RECONSIDERATION)
3. Unlock review for formal appeal (APPEAL)

Remove option 1 (GENERAL) entirely.
Keep options 2 and 3 only.

CHANGE 2 — Change modal header text
Find the modal header/title bar.
BEFORE: "CAS RiskReview" or similar
AFTER: "Unlock Review"

CHANGE 3 — Match header color to Review Details
The Review Details section header uses a specific 
color (likely text-[color:var(--brand-primary)] 
or a teal/green color based on the screenshot).
Find the exact color class used on the 
Review Details section header and apply the same 
color to the Unlock Review modal header.

Show exact before/after for each of the 3 changes.
No backend changes.