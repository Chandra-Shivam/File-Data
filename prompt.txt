Read AGENTS.md and UI-PATTERNS.md first.

Open this file:
backend/src/Casrr.Infrastructure/SqlServer/
SqlReviewRepository.cs

Find the private method GetReviewHeaderByIdAsync.
Show me every column in its SELECT list with 
ordinal numbers, like this:
0: r.[Review_id]
1: r.[eCIF_number]
... and so on until the last column.

Then answer YES or NO:
Is r.[Sample_type] in this SELECT list?
Is r.[Sample_criteria] in this SELECT list?

If NO for either — make these changes:

1. Add to SELECT list in GetReviewHeaderByIdAsync,
   GetLatestReviewHeaderForSampleAndEcifAsync, and
   GetLatestReviewHeaderForEcifAsync:
   r.[Sample_type],
   r.[Sample_criteria]

2. Add to ReviewHeader class:
   public string? SampleType { get; init; }
   public string? SampleCriteria { get; init; }

3. Add to SqlDataReader at next available ordinal N:
   SampleType = rdr.IsDBNull(N) 
     ? null : rdr.GetString(N),
   SampleCriteria = rdr.IsDBNull(N+1) 
     ? null : rdr.GetString(N+1)

4. In GetReviewByEcifAsync and 
   GetReviewByKeysAsync where ReviewInfoSection 
   is constructed, add:
   SampleType = row?.SampleType,
   SampleTarget = row?.SampleCriteria,

5. In Casrr.Domain/ReviewQueue.cs, check 
   ReviewInfoSection — if SampleType and 
   SampleTarget are missing, add:
   public string? SampleType { get; init; }
   public string? SampleTarget { get; init; }

Show exact before/after for every change.
No frontend changes.