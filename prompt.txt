Read AGENTS.md and UI-PATTERNS.md first.

Backend only. No frontend changes.

Only modify:
backend/src/Casrr.Infrastructure/SqlServer/
SqlReviewRepository.cs

ISSUE: Sample_type column exists in 
dbo.[02_CORE_02_Reviews] but is NOT being 
selected in the ReviewHeader queries. 
Sample_target (Sample_criteria) is also missing.

STEP 1 — Read and show the current SELECT list 
for GetReviewHeaderByIdAsync. Identify what 
ordinal index comes after the last existing column.

STEP 2 — Add these two columns to ALL THREE 
ReviewHeader SELECT queries:
  r.[Sample_type],
  r.[Sample_criteria]

Add them at the end of the SELECT list, after 
the existing new fields (Prior_CAS_review_id etc.)

STEP 3 — Add properties to ReviewHeader class:
  public string? SampleType { get; init; }
  public string? SampleCriteria { get; init; }

STEP 4 — Map in SqlDataReader for all three queries:
  SampleType = rdr.IsDBNull(N) ? null : rdr.GetString(N),
  SampleCriteria = rdr.IsDBNull(N+1) ? null : rdr.GetString(N+1)

Where N is the next ordinal after existing columns.

STEP 5 — Map to ReviewInfoSection in both 
GetReviewByEcifAsync and GetReviewByKeysAsync:

BEFORE:
  SecondReviewer = row?.MentorName,

AFTER:
  SecondReviewer = row?.MentorName,
  SampleType = row?.SampleType,
  SampleTarget = row?.SampleCriteria,

STEP 6 — Verify ReviewInfoSection in 
Casrr.Domain already has SampleType and 
SampleTarget properties. If not, add:
  public string? SampleType { get; init; }
  public string? SampleTarget { get; init; }

Show exact before/after for every change.
No frontend changes.