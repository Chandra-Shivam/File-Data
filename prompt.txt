Read AGENTS.md and UI-PATTERNS.md first.

Check these files and report findings only 
(no changes yet):

1. backend/src/Casrr.Infrastructure/SqlServer/
   SqlReviewRepository.cs
   - Does GetReviewByKeys or equivalent return 
     these fields:
     * Prior_CAS_review_id → priorReviewId
     * Prior_CAS_review_date → priorReviewDate
     * Review_initial_approval_date → initialApproval
     * Mentor_name → secondReviewer
     * Mentor_review_date → secondReviewDate

2. frontend/src/app/review/[ecif]/review-info/
   components/sections/hooks/useReviewInfo.ts
   - Does the reviewInfo status section map these 
     5 fields? Show the current status field mappings.

3. frontend/src/app/review/[ecif]/review-info/
   components/sections/ReviewInfoSection.tsx
   - Are these 5 fields being displayed?
     * Prior Review ID
     * Prior Review Date  
     * Initial Approval
     * Second Reviewer
     * Second Review Date
   - Show the current JSX for these fields.

4. For Prior Review ID and Prior Review Date:
   The legacy logic is:
   - Prior Review Date = MAX(Review_finalized_date) 
     for same Customer_number in Reviews table 
     (excluding current review)
   - Prior Review ID = Review_id WHERE 
     Review_finalized_date = Prior Review Date 
     AND Customer_number = same customer
   
   Check: are Prior_CAS_review_id and 
   Prior_CAS_review_date already stored as columns 
   in dbo.[02_CORE_02_Reviews]? If yes, are they 
   being read by the backend?

Report exactly what is mapped vs missing.
Do NOT make any changes yet.