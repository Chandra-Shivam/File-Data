Read AGENTS.md and UI-PATTERNS.md first.

All 5 fields exist in dbo.[02_CORE_02_Reviews] but 
are not read by the backend or mapped to the frontend.
Wire them end-to-end following Clean Architecture rules.

STAGE 1 — Backend only. No frontend changes yet.

Fields to add:
- Prior_CAS_review_id → priorReviewId (int or string)
- Prior_CAS_review_date → priorReviewDate (string/date)
- Review_initial_approval_date → initialApproval (string/date)
- Mentor_name → secondReviewer (string)
- Mentor_review_date → secondReviewDate (string/date)

STEP 1 — Find and read these files:
1. The ReviewHeader class — find its file path 
   and show current properties
2. SqlReviewRepository.cs — show the SELECT 
   query for GetReviewHeaderByIdAsync
3. The ReviewForm or ReviewInfo contract/DTO 
   in Casrr.Application — show current properties

STEP 2 — Add to ReviewHeader class:
Add the 5 new properties matching existing 
property types and naming conventions in that class.

STEP 3 — Add to SQL SELECT:
In GetReviewHeaderByIdAsync (and any other 
ReviewHeader SELECT queries), add the 5 columns:
  ,[Prior_CAS_review_id]
  ,[Prior_CAS_review_date]
  ,[Review_initial_approval_date]
  ,[Mentor_name]
  ,[Mentor_review_date]

Map them to ReviewHeader properties in the 
SqlDataReader mapping section.

STEP 4 — Add to ReviewInfo contract/DTO:
In the Application layer contract that exposes 
ReviewInfo to the frontend, add these 5 fields 
so they are included in the API response.

STEP 5 — Map in the service/repository:
Ensure the 5 new ReviewHeader properties flow 
through to the ReviewInfo contract returned by 
GetReviewByKeys.

Show exact before/after for every file changed.
Use raw ADO.NET (no EF) per AGENTS.md.
No frontend changes yet.