Read UI-PATTERNS.md from the root of the project.

Then look at these existing maintenance pages and their components:
- frontend/src/app/maintenance/covenants/page.tsx
- frontend/src/app/maintenance/cas-findings/page.tsx

For each pattern in UI-PATTERNS.md, compare it against what is 
actually used in those pages and report back:

1. What matches exactly
2. What is different (class names, markup structure, component 
   composition, state management approach)
3. What exists in the actual pages but is missing from UI-PATTERNS.md
4. What is in UI-PATTERNS.md but not used anywhere in the actual codebase

Be specific — show the actual class/code from the repo vs what 
UI-PATTERNS.md says. Do not summarize vaguely.