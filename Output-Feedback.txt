STOP. Change approach entirely.

The overflow-x-hidden on AppShell's main element 
cannot be overridden without modifying AppShell.
We will avoid the conflict by making the table 
fit without horizontal scroll on smaller screens.

Only modify frontend/src/app/review-queue/page.tsx

FIX — Hide secondary columns on laptop screens:

In the columns array, add responsive hiding to 
Bank PD and CAS PD columns. These are secondary 
numeric columns that can be hidden on smaller 
screens per UI-PATTERNS.md.

For Bank PD column header and cell — add hidden 
class for smaller screens:
  header className: "hidden xl:table-cell"
  cell className: "hidden xl:table-cell"

For CAS PD column header and cell — same:
  header className: "hidden xl:table-cell"  
  cell className: "hidden xl:table-cell"

Also remove all min-w-[700px] and minWidth style 
constraints from the scroll container — they are 
no longer needed since the table will fit on laptop 
screens without horizontal scroll.

The table width without Bank PD and CAS PD will be 
approximately 750px which fits comfortably in the 
available right panel space on a laptop with the 
sidebar expanded.

On xl screens (1280px+) all columns show. On 
smaller screens Bank PD and CAS PD are hidden.

Show exact before/after for the columns array.
No backend changes.