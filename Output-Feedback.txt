STOP. Only modify the positioning of the Unlock Review 
button in:
frontend/src/app/review/[ecif]/review-info/components/
sections/ReviewInfoSection.tsx

Current problem: The button sits below the entire 
Review Status section as a full-width block (w-full, mt-4).

Fix: Place the button inside the Review Status grid, 
in the empty grid cell after "INITIAL APPROVAL" 
(row 2, 4th column of the 4-column grid).

BEFORE (current — full width below section):
{s?.mgrApproval && (
  <button
    type="button"
    onClick={() => setUnlockOpen(true)}
    className="inline-flex items-center justify-center 
    w-full rounded-md bg-sky-500 hover:bg-sky-600 
    text-white text-sm font-semibold px-4 py-3 
    shadow mt-4"
  >
    Unlock Review
  </button>
)}

AFTER (inside grid, aligned with other fields):
- Remove the standalone full-width button block
- In the grid row that contains INITIAL APPROVAL, 
  add a 4th cell with the button:

<div className="flex items-end">
  {s?.mgrApproval && (
    <button
      type="button"
      onClick={() => setUnlockOpen(true)}
      className="inline-flex items-center justify-center 
      rounded-md bg-sky-500 hover:bg-sky-600 text-white 
      text-sm font-semibold px-4 py-2.5 shadow w-full"
    >
      Unlock Review
    </button>
  )}
</div>

Show exact before/after of the grid row containing 
INITIAL APPROVAL and the surrounding markup.
No other changes. No backend changes.