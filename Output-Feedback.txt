STOP. Only fix the scrolling layout issue in:
frontend/src/app/review-queue/page.tsx
AND check frontend/src/components/ for the DataTable 
component file.

Issue: The "Draft Completed" section header bar does 
not extend to full width when the table scrolls 
horizontally. The header is outside the scroll 
container so it stays at container width while the 
table extends wider.

Before fixing, read the DataTable component and show:
1. How does DataTable render its section header 
   relative to the scroll container?
2. Is the header inside or outside the overflow-x-auto 
   div?

Then apply the correct fix based on what you find:

Option A — If DataTable renders header OUTSIDE scroll div:
Move overflow-x-auto to the outer DataTable wrapper 
so both header and table scroll together:
- Remove overflow-x-auto from the inner table div
- Add overflow-x-auto to the outermost DataTable wrapper
- Add min-w-full to the header div so it always spans 
  full table width

Option B — If DataTable accepts a prop for this:
Pass the appropriate prop to make the header scroll 
with the table content.

Option C — If DataTable cannot be modified:
Wrap the entire DataTable in a div with overflow-x-auto 
and ensure the section title bar has a min-width that 
matches the table's min-width.

The fix must ensure:
- Header and table always scroll together as one unit
- Header always spans the full width of table content
- Scrollbar appears once at the bottom for the whole panel

No backend changes. Show exact before/after.