STOP. Only modify:
frontend/src/app/review-queue/page.tsx

The "Open" button in the last column is getting 
clipped on smaller laptop screens.

Before making changes, check:
1. Does the table wrapper div have overflow-x-auto 
   or overflow-auto? Show the current class.
2. What is the width/minWidth set on the "Open" 
   column definition? Show the columns array.

Then apply these two fixes:

FIX 1 — Ensure horizontal scroll works on small screens
The table wrapper must have overflow-x-auto so users 
can scroll horizontally when the table is wider than 
the viewport:

If current class is "overflow-auto":
  Change to "overflow-x-auto"
If it already has "overflow-x-auto":
  Leave it — the issue is column widths instead.

FIX 2 — Make the Open button more compact
The "Open" button is pushing the table too wide.
Replace the full text "Open" button with a compact 
icon + text that shrinks gracefully:

Change the Open column definition:
- Set a fixed small width on the column: width: 70
- Change button text from "Open" to just "Open" but 
  with compact padding:
  
  className="inline-flex items-center justify-center 
  rounded bg-[#1F3864] text-white text-xs font-semibold 
  px-2 py-1 cursor-pointer select-none 
  whitespace-nowrap hover:opacity-90"

The whitespace-nowrap prevents the button text from 
wrapping which can cause row height issues.

FIX 3 — Set explicit column widths to prevent overflow
In the columns array, set explicit widths to control 
the table layout:
- eCIF #: width: 80
- Reviewer: width: 160  
- Exposure: width: 120
- Bank PD: width: 80
- CAS PD: width: 80
- Completed: width: 100
- Open (action): width: 70

Show exact before/after for the columns array and 
the table wrapper div class.
No backend changes.