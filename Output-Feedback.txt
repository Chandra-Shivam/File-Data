In LoanCodeTable.tsx on line 73, the key={r.original.code} 
is causing a duplicate key React error for codes O1E1 and O1E2.

Fix 1 — Frontend: change the key to use index as fallback:
key={`${r.original.code}-${index}`}

Fix 2 — Backend: check SqlLoanCodeRepository.GetLibraryAsync 
SQL query. Add DISTINCT or verify there is no accidental 
duplication in the SELECT. The query should return each 
Loan_code exactly once.

Provide both fixes.