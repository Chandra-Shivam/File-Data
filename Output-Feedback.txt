Read AGENTS.md and UI-PATTERNS.md first.

Read this file in full:
frontend/src/app/review/[ecif]/review-info/components/
sections/ReviewInfoSection.tsx

Find the "Unlock Review" button and answer:
1. What is the current visibility condition on the button?
   (Is it conditionally rendered? What condition controls it?)
2. What field/value represents Manager Approval Date 
   in this component? (check useReviewInfo hook too)
3. Is the button currently hidden because the condition 
   is wrong, missing, or because mgrApproval is null 
   in the current test data?

Then make this fix:
- The Unlock Review button must be visible ONLY when 
  Manager Approval Date (mgrApproval) is populated 
  (not null, not empty string)
- If the button already has this condition, show me 
  the exact line and explain why it is not working
- If the condition is missing, add:
  {mgrApproval && (
    <Button ...>Unlock Review</Button>
  )}
  where mgrApproval is the correct field name from 
  useReviewInfo

Also verify:
- Clicking Unlock Review opens the radio option popup ✓
- Clicking outside popup without selecting restores the button ✓  
- Selecting option 2 opens Reconsideration modal ✓
- Selecting option 3 opens Appeal modal ✓

Only modify ReviewInfoSection.tsx if a fix is needed.
Show exact before/after.
No backend changes.