Implemented the RichTextEditor state fix in frontend/src/app/maintenance/help-tips/page.tsx as requested.

Summary of changes:
- Introduced local editor state:
  - Added: const [newContent, setNewContent] = useState<string>(newItem.helpTipContent ?? '')
  - Added sync: useEffect(() => { setNewContent(newItem.helpTipContent ?? '') }, [newItem.helpTipContent])
- Bound RichTextEditor to local state:
  - value={newContent}
  - onChange={(html) => setNewContent(html)}
- Updated create handler to use the local editor state:
  - const helpTipContent = newContent ?? ''
- Reset local editor state after successful create:
  - setNewContent('')
- No other files were modified.

Behavioral result:
- Avoids recreating the entire newItem object on each keystroke, preventing caret jumps.
- Enter key reliably inserts a new line in the rich text editor.
- Conforms to patterns used elsewhere (e.g., CustomerInfoSection, RegulatoryTrackingSection).