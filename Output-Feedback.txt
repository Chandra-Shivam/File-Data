Applied both requested fixes.

Frontend fix (duplicate React key):
- File: frontend/src/app/maintenance/loan-codes/components/LoanCodeTable.tsx
- Changes:
  - Updated map signature to receive index: .map((r, index) => (
  - Updated row key to include index fallback: key={`${r.original.code}-${index}`}
- Outcome: Eliminates duplicate key warnings for codes like O1E1 and O1E2 while maintaining stable keys across rerenders in this table.

Backend fix (deduplicate query results):
- File: backend/src/Casrr.Infrastructure/SqlServer/SqlLoanCodeRepository.cs
- Method: GetLibraryAsync
- Changes:
  - Adjusted SELECT to trim and deduplicate rows:
    SELECT DISTINCT LTRIM(RTRIM([Loan_code])) AS [Loan_code], LTRIM(RTRIM([Loan_code_category])) AS [Loan_code_category], [Loan_code_description]
    FROM dbo.[03_LIBRARY_07_Loan Codes] WITH (NOLOCK)
    WHERE (@cat IS NULL OR LTRIM(RTRIM([Loan_code_category])) = LTRIM(RTRIM(@cat)))
    ORDER BY [Loan_code_category], [Loan_code];
- Rationale:
  - DISTINCT prevents accidental duplicates caused by whitespace or redundant rows.
  - LTRIM/RTRIM normalizes trailing/leading spaces in Loan_code and Loan_code_category, improving dedup reliability while preserving category filter semantics.
  - Ordering unchanged.

Validation steps:
- Backend:
  - Hit GET /api/v1/loan-codes and /api/v1/loan-codes?category=SomeCategory and verify each Loan_code appears once.
  - If your data contains the same Loan_code with differing descriptions or categories, DISTINCT may still return more than one row. In that rare case, consider a stronger shape to enforce “one row per code”, e.g.:
    SELECT LTRIM(RTRIM([Loan_code])) AS [Loan_code],
           MIN(LTRIM(RTRIM([Loan_code_category]))) AS [Loan_code_category],
           MAX([Loan_code_description]) AS [Loan_code_description]
    FROM dbo.[03_LIBRARY_07_Loan Codes] WITH (NOLOCK)
    WHERE (@cat IS NULL OR LTRIM(RTRIM([Loan_code_category])) = LTRIM(RTRIM(@cat)))
    GROUP BY LTRIM(RTRIM([Loan_code]));
  - Current change follows the requested “add DISTINCT” approach.
- Frontend:
  - Navigate to /maintenance/loan-codes. Confirm no React duplicate key warnings and table renders correctly for O1E1 and O1E2.

Note:
- The editor surfaced a TypeScript deprecation warning for baseUrl in frontend/tsconfig.json. This is unrelated to the fix and can be silenced per TS guidance by setting "ignoreDeprecations": "6.0" if desired.

Both fixes are in place and ready for verification.