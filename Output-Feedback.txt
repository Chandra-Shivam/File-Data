STOP. Do not summarize anything. Do not touch backend files.

Apply this exact minimal fix to 
frontend/src/components/ui/RichTextEditor.tsx only.

TWO changes, nothing else:

CHANGE 1 — Find the useEffect that sets innerHTML and 
add a guard so it only writes when content differs:

BEFORE:
useEffect(() => {
  if (editorRef.current) {
    editorRef.current.innerHTML = value;
  }
}, [value]);

AFTER:
useEffect(() => {
  const el = editorRef.current;
  if (el && el.innerHTML !== value) {
    el.innerHTML = value ?? '';
  }
}, [value]);

CHANGE 2 — Find handleInput and remove the manual 
caret save/restore entirely:

BEFORE:
const handleInput = () => {
  const caret = saveCaretPosition();
  onChange(editorRef.current!.innerHTML);
  restoreCaretPosition(caret);
};

AFTER:
const handleInput = () => {
  const el = editorRef.current;
  if (el) onChange(el.innerHTML);
};

Rules:
- Do NOT remove any existing props (readOnly, showToolbar, 
  ariaLabel, minHeight must all remain)
- Do NOT rewrite or replace the whole file
- Do NOT touch any other file
- Show exact before/after lines only