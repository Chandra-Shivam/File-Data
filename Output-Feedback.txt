STOP. Do not touch the button itself. Read 
ReviewInfoSection.tsx and look specifically at what 
WRAPS the 4-column Review Status grid.

Find and answer:
1. Is the grid inside a <fieldset> element? 
   If yes — fieldset with disabled attribute blocks 
   ALL clicks including buttons inside it.
2. Does any parent div have pointer-events-none?
3. Is there a transparent overlay div positioned 
   absolutely over the grid?
4. Does the grid container have any onClick handler 
   that might be consuming the event?

Show the exact JSX from the outermost wrapper of 
Review Status down to the grid div.

Then apply the correct fix:
- If wrapped in <fieldset disabled> → move the 
  Unlock Review button OUTSIDE the fieldset, but 
  still visually positioned inside the Review Status 
  card using absolute positioning or by restructuring 
  the card layout
- If pointer-events-none on parent → add 
  pointer-events-auto on the button's wrapper div
- If overlay div → move button above the overlay 
  in the DOM

The button worked at full-width below the section. 
That position was OUTSIDE whatever is blocking clicks.
The fix must place the button where clicks work — 
either outside the blocking parent or with the 
blocking properly overridden.

No backend changes. Show exact before/after.