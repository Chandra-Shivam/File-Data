STOP. Only fix the layout in:
frontend/src/app/review-queue/page.tsx

Issue: When the sidebar is expanded, the Draft 
Completed panel does not fill available space — 
it appears cut off on the left. When sidebar collapses 
it works. The panel needs to fill remaining space 
in BOTH states.

Before fixing, show me:
1. The current CSS classes on the two-column wrapper 
   div that contains Progress Status (left) and 
   Draft Completed (right)
2. The CSS classes on the Draft Completed panel div
3. Is the layout using flex or grid?

Then apply this fix:
- The two-column layout should use flex with the 
  right panel filling remaining space:

  Outer wrapper:
  className="flex gap-4 items-start"

  Left panel (Progress Status):
  className="w-[300px] shrink-0 ..."
  (fixed width, never shrinks)

  Right panel (Draft Completed):
  className="flex-1 min-w-0 ..."
  (fills ALL remaining space, min-w-0 prevents 
  overflow issues in flex)

The min-w-0 on the right panel is critical — 
without it, flex children don't shrink below their 
content size and the panel won't fill space correctly.

Also fix: theadClassName is currently "bg-slate-800 
text-white" — change to "bg-[#1F3864] text-white" 
to match the brand blue used on the section header.

Show exact before/after for the layout wrapper 
and both panel divs.
No backend changes.