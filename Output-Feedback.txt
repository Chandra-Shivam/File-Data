Read AGENTS.md and UI-PATTERNS.md first.

Working on frontend/src/app/maintenance/loan-codes — 
apply these 4 improvements:

**1. Disabled fields should look read-only**
All Select and input fields in the table rows should be 
visually distinct when disabled={true} (non-edit mode).
Check what disabled styling the existing Select and input 
components support in this project. Apply classes that make 
them look read-only — muted background, muted text, no border 
appearance. Do not invent new components.

**2. cursor-not-allowed on disabled fields**
All disabled inputs and Select components in table rows must 
show cursor-not-allowed on hover. Check how existing 
maintenance pages (covenants/page.tsx) handle this and 
match that pattern exactly.

**3. Add new category option in the Add New form dropdown**
In the Add New Loan Code form, the Category select must 
include an "＋ Add new category..." option at the bottom 
of the dropdown list. When selected:
- Replace the select with a text input
- User types the new category name
- A small Cancel link reverts back to the select
- The typed value is used as the category on Create
Check if a Combobox or creatable select component already 
exists under frontend/src/components/ui/ before building 
anything new. If one exists, use it. If not, implement 
the toggle pattern described above using only existing 
components.

**4. Pagination**
Check the existing codebase for a Pagination component:
- Look in frontend/src/components/ui/ for any Pagination 
  or paging component
- Look at other pages with large datasets for existing 
  pagination patterns
If a Pagination component exists, use it to paginate the 
loan codes table (20 rows per page).
If none exists, implement a simple page prev/next pattern 
using Button components that matches the project's existing 
style — do not install any new packages.

For each item, show what existing component or pattern you 
found in the codebase before writing any new code.