STOP. Do not summarize. Do not touch any backend files.
Do not rewrite the whole file.

Apply these two targeted fixes to 
frontend/src/components/ui/RichTextEditor.tsx only:

FIX 1 — Remove dangerouslySetInnerHTML from the 
contentEditable div.

BEFORE:
<div
  ref={editorRef}
  contentEditable
  suppressContentEditableWarning
  dangerouslySetInnerHTML={{ __html: value }}
  onInput={handleInput}
/>

AFTER:
<div
  ref={editorRef}
  contentEditable
  suppressContentEditableWarning
  onInput={handleInput}
/>

FIX 2 — Verify the useEffect has the inequality guard.
It must look exactly like this — if it already does, 
leave it alone. If it doesn't, fix it:

useEffect(() => {
  const el = editorRef.current;
  if (el && el.innerHTML !== value) {
    el.innerHTML = value ?? '';
  }
}, [value]);

These are the only two changes.
Do NOT remove readOnly, showToolbar, ariaLabel, 
minHeight props.
Do NOT rewrite the file.
Show exact before/after for each change.