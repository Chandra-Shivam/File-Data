STOP. Only modify:
frontend/src/app/review-queue/page.tsx

Add an "Open" button in the last column of the review 
table that opens the Review Form for that row.

Before making any changes:
1. Find the handleOpen function — show its signature
2. Find where the Borrower Name click triggers 
   handleOpen — show that code
3. Find the column definitions for the DataTable — 
   show the current columns array

Then add the Open button:
- Add a new column at the end of the columns array 
  after "Completed"
- Column header: empty string ""
- Cell: a small "Open" button that calls handleOpen(row)
- The button must use a <div role="button"> NOT 
  a native <button> (fieldset disabled issue applies 
  here too if present — check)
- Styling must match the existing page style:
  - Look at what other action elements on this page 
    use for their classes
  - Use bg-[#1F3864] text-white text-xs font-semibold 
    px-3 py-1 rounded hover:opacity-90 cursor-pointer
  - Keep it compact — same size as the existing 
    status badges on the page

- e.stopPropagation() on click to prevent row 
  double-click from also firing

Show exact before/after.
No backend changes.