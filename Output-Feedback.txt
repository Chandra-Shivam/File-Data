STOP. The flex approach is not working — the scroll 
content is showing the wrong portion.

Revert the two-column layout change and use CSS Grid 
with explicit sizing instead of flex.

Only modify frontend/src/app/review-queue/page.tsx

REVERT AND REPLACE the layout wrapper:

BEFORE (current flex approach — not working):
<div className="mt-4 flex gap-4 items-start">
  <aside className="w-[300px] shrink-0">
  <section className="flex-1 min-w-0">

AFTER (use explicit CSS Grid instead):
<div className="mt-4 grid gap-4 items-start" 
  style={{ gridTemplateColumns: '300px 1fr' }}>
  <aside>
  <section className="min-w-0 overflow-hidden">

Why this works:
- grid-template-columns: 300px 1fr explicitly gives 
  the left panel 300px and the right panel ALL 
  remaining space (1fr)
- 1fr is dynamic — it recalculates whenever the 
  sidebar expands or collapses
- min-w-0 overflow-hidden on the right section 
  prevents the grid cell from overflowing its 
  allocated 1fr space
- This approach does not conflict with the inner 
  overflow-x-auto scroll container

Also ensure the inner scroll container on the 
right panel uses:
<div className="overflow-x-auto">
  (no w-full needed — the grid cell already 
  determines the width)

Show exact before/after.
No backend changes.