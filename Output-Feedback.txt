Root cause confirmed: <fieldset disabled> in page.tsx 
wraps ActiveComponent and disables all button clicks 
in read-only mode.

Fix using Option B — lift state to page.tsx:

In page.tsx:
1. Add state: const [unlockOpen, setUnlockOpen] = useState(false)

2. Find where mgrApproval is available in page.tsx 
   (it's likely in the review data fetched by the page). 
   Show me where it comes from.

3. Render the Unlock Review button OUTSIDE the fieldset, 
   just before or after it:
   {mgrApproval && !isEditing && (
     <div className="flex justify-end mb-2">
       <button
         type="button"
         onClick={() => setUnlockOpen(true)}
         className="inline-flex items-center justify-center 
         rounded-md bg-sky-500 hover:bg-sky-600 text-white 
         text-xs font-semibold px-3 py-1.5 shadow"
       >
         Unlock Review
       </button>
     </div>
   )}
   {isEditing ? (
     <ActiveComponent />
   ) : (
     <fieldset disabled className="contents">
       <ActiveComponent />
     </fieldset>
   )}

4. Pass unlockOpen and setUnlockOpen as props to 
   ReviewInfoSection (through ActiveComponent or 
   directly):
   - Add props to ReviewInfoSection: 
     unlockOpen?: boolean
     onUnlockOpenChange?: (open: boolean) => void
   - In ReviewInfoSection, use these props instead 
     of local state for unlockOpen

5. Remove the Unlock Review button from 
   ReviewInfoSection.tsx entirely (it now lives in 
   page.tsx)

6. Keep all modal JSX (unlock options modal, 
   reconsideration modal, appeal modal) in 
   ReviewInfoSection.tsx — only the trigger button 
   moves to page.tsx

Show exact before/after for both files.
No backend changes.