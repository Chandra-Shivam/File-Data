Read AGENTS.md and UI-PATTERNS.md first.

Make these 4 changes. Files to modify:
1. frontend/src/app/review/[ecif]/review-info/
   components/ui.tsx (Field component — Task 1)
2. frontend/src/app/review/[ecif]/review-info/
   components/sections/ReviewInfoSection.tsx 
   (Tasks 2, 3, 4)

TASK 1 — Disabled cursor on view mode fields
In the Field component (ui.tsx), the view mode input:
  className="w-full h-9 border border-slate-200 
  rounded px-2 text-sm bg-slate-50"

Add cursor-not-allowed and the full disabled styling:
  className="w-full h-9 border border-slate-200 
  rounded px-2 text-sm bg-slate-100 text-slate-500 
  cursor-not-allowed"

TASK 2, 3 — Replace Reviewer Name and Manager with 
dropdowns in edit mode

In ReviewInfoSection.tsx:
1. Import listUsers and CasUser from 
   "@/services/api/casUsers"
2. Import Select from "@/components/ui/Select"
3. Add state for fetched users:
   const [croUsers, setCroUsers] = useState<CasUser[]>([])
   const [managerUsers, setManagerUsers] = useState<CasUser[]>([])

4. Fetch users once on mount:
   useEffect(() => {
     listUsers().then((users) => {
       setCroUsers(users.filter(u => 
         u.role === 'CRO' && u.active))
       setManagerUsers(users.filter(u => 
         u.role === 'Manager' && u.active))
     }).catch(() => {})
   }, [])

5. Replace the Reviewer Name Field with:
   {isEditing ? (
     <div className="flex flex-col gap-1">
       <label className="text-xs text-slate-500 
         font-medium uppercase tracking-wider">
         REVIEWER NAME
       </label>
       <Select
         size="sm"
         className="bg-white w-full"
         value={a?.reviewerName ?? ""}
         onChange={(e) => {
           const user = croUsers.find(
             u => u.name === e.target.value)
           changes?.setField("reviewInfo", 
             "reviewerName", e.target.value)
           if (user) {
             changes?.setField("reviewInfo", 
               "reviewerEmail", user.email)
           }
         }}
       >
         <option value="">Select reviewer...</option>
         {croUsers.map(u => (
           <option key={u.id} value={u.name}>
             {u.name}
           </option>
         ))}
       </Select>
     </div>
   ) : (
     <Field label="REVIEWER NAME" 
       value={a?.reviewerName ?? ""} />
   )}

6. Replace the Manager Field with identical pattern 
   using managerUsers and these field names:
   - section: "reviewInfo"  
   - name field: "managerName"
   - email field: "managerEmail"
   - label: "MANAGER"
   - placeholder: "Select manager..."

TASK 4 — Email auto-update is already handled in 
Task 2 and 3 above via changes.setField for email.
Verify Reviewer Email and Manager Email fields 
use the same section/name as what setField targets.
Show the current field names for reviewerEmail 
and managerEmail in the component.

Do NOT modify any other fields.
No backend changes.
Show exact before/after for each change.