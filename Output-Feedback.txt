Read AGENTS.md and UI-PATTERNS.md first.

IMPORTANT: Do NOT start from Stage 1. The backend is fully 
complete and must not be touched. These are frontend-only 
UI improvements to files that already exist in the repo.

Files to modify (frontend only):
- frontend/src/app/maintenance/loan-codes/components/LoanCodeTable.tsx
- frontend/src/app/maintenance/loan-codes/page.tsx

Make these 4 changes:

1. DISABLED FIELDS — READ-ONLY APPEARANCE
In LoanCodeTable.tsx, all Select and input fields in table 
rows are disabled when not in edit mode. They currently look 
editable. Check how covenants/page.tsx styles its disabled 
inputs and match that pattern. Apply the same disabled 
styling here.

2. CURSOR NOT-ALLOWED ON DISABLED FIELDS
All disabled inputs and Select fields in table rows must 
show cursor-not-allowed on hover. Again match covenants/page.tsx 
exactly.

3. ADD NEW CATEGORY OPTION IN CREATE FORM
In page.tsx, the Category dropdown in the Add New Loan Code 
form must have an "＋ Add new category..." option at the 
bottom. When selected:
- Hide the select, show a text input in its place
- User types new category name
- Show a small "Cancel" link that reverts back to the select
- The typed value is used as the Category on Create
Before writing anything new, check 
frontend/src/components/ui/ for an existing Combobox or 
creatable select. If found, use it. If not, implement 
the toggle pattern above using only existing components.

4. PAGINATION
Before writing anything, check:
- frontend/src/components/ui/ for a Pagination component
- Other pages in the project that paginate large datasets
Report what you find, then implement pagination for the 
loan codes table at 20 rows per page using whatever 
already exists. Do not install new packages.

Show the exact code changes only — no backend files, 
no Stage 1 summary.