Read AGENTS.md and UI-PATTERNS.md first.

Working on the Review Form Review Info page.
Before making any changes, read and report:

1. Read ReviewInfoSection.tsx — how are the 
   Reviewer Name and Manager fields currently 
   rendered in BOTH view mode and edit mode?
   Show the exact JSX for those fields.

2. Does a backend endpoint for CAS users already 
   exist? Search:
   - backend/src/Casrr.Api/Controllers/ for 
     "cas-users" or "CasUsers" or "users"
   - frontend/src/services/api/ for any file 
     that fetches users by role

3. Read the existing maintenance page for CAS Users 
   if it exists:
   frontend/src/app/maintenance/cas-users/
   Show what API endpoint it uses to fetch users.

4. What fields does the CAS users service/type 
   expose? (name, email, role, etc.)

Report all findings. Do NOT make any changes yet.