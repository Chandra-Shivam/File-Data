STOP. Only modify the Unlock Review trigger element in 
frontend/src/app/review/[ecif]/review-info/components/
sections/ReviewInfoSection.tsx

Keep the button in the empty grid cell (4th column, 
row 2 after INITIAL APPROVAL).

The fieldset disabled in page.tsx blocks <button> 
elements but NOT <div> elements.

Replace the <button> with a <div role="button">:

BEFORE:
<div className="flex items-end">
  {s?.mgrApproval && (
    <button
      type="button"
      onClick={...}
      className="..."
    >
      Unlock Review
    </button>
  )}
</div>

AFTER:
<div className="flex items-end">
  {s?.mgrApproval && (
    <div
      role="button"
      tabIndex={0}
      onClick={() => setUnlockOpen(true)}
      onKeyDown={(e) => {
        if (e.key === 'Enter' || e.key === ' ') 
          setUnlockOpen(true)
      }}
      className="inline-flex items-center 
      justify-center rounded-md bg-sky-500 
      hover:bg-sky-600 text-white text-xs 
      font-semibold px-3 py-1.5 shadow 
      cursor-pointer select-none w-full"
    >
      Unlock Review
    </div>
  )}
</div>

No other changes. No backend changes.