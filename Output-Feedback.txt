STOP. Do not change the two-column layout again.

The issue persists across flex and grid approaches.
The root cause is NOT in the two-column layout —
it must be in a parent container or scroll behavior.

Read these files and report findings only 
(no changes yet):

1. Find the layout file that wraps the Review Queue 
   page — search for:
   frontend/src/app/review-queue/layout.tsx
   OR the closest parent layout.tsx in the app/ tree
   Show its full JSX and CSS classes.

2. Read frontend/src/components/table/DataTable.tsx 
   in full. Check:
   - Does it set scrollLeft on mount or on data change?
   - Does it have any useEffect that scrolls 
     horizontally?
   - Does it have any scroll-related refs?

3. In review-queue/page.tsx, check:
   - Is there any useEffect that scrolls the 
     table container?
   - Does any container have dir="rtl" or 
     direction: rtl?

4. In the browser DevTools (ask the developer to 
   check): What is the computed width of the 
   <section className="min-w-0 overflow-hidden"> 
   element when the sidebar is expanded?

Report what you find. Do NOT make any changes.
The fix depends entirely on what you find here.