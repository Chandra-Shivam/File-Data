Reassessment complete after your updates.

Conclusion
AGENTS.md is now largely aligned with the repository’s actual structure and conventions. The major previously noted mismatches (authentication model, ORM/repository pattern, API versioning, folder layout, CI folders) have been corrected to reflect the codebase. One material discrepancy remains in the frontend dependency policy.

What matches now (with evidence)
- Monorepo and folders
  - Document lists frontend/, backend/, docs/, discovery/, pipelines/, azure-pipelines/, scripts/, tools/, legacy/. All are present at the root.

- Authentication (Azure AD JWT + policies)
  - AGENTS.md explicitly documents Azure AD JWT Bearer via Microsoft.Identity.Web and named policies.
  - Code wires Microsoft.Identity.Web / JWT bearer and policies:
    - backend/src/Casrr.Api/Program.cs and backend/src/Casrr.Api/Extensions/StartupExtensions.cs (AddMicrosoftIdentityWebApi / AddJwtBearer, AddAuthorizationPolicies).
  - Controllers use policy-based authorization:
    - Example: backend/src/Casrr.Api/Controllers/CovenantsController.cs → [Authorize(Policy = "RequireActiveUser")].
  - Swagger security schemes reflect Bearer and Graph tokens.

- Data access pattern (raw ADO.NET + optional Dapper)
  - AGENTS.md states repositories use SqlClient/Dapper; EF DbContext exists but is not the default repository pattern.
  - Code uses raw ADO.NET:
    - backend/src/Casrr.Infrastructure/SqlServer/SqlCovenantsRepository.cs uses SqlConnection/SqlCommand/SqlParameter.
  - Dapper.dll appears in API bin output.

- Project structure and layering
  - AGENTS.md now states 4 core projects + tests; repo has Casrr.Api, Casrr.Application, Casrr.Domain, Casrr.Infrastructure plus test projects under backend/Unit Tests/.
  - Application interfaces live at the project root (not under Interfaces/) — matches AGENTS.md.
  - Contracts/DTOs live under Casrr.Application/Reviews/Contracts/ — matches AGENTS.md’s “Contracts” location.
  - Infrastructure repositories under Casrr.Infrastructure/SqlServer/ — matches AGENTS.md.

- API design and versioning
  - AGENTS.md standardizes on /api/v1/{resource} and includes /categories and /types endpoints.
  - Code uses versioned routes: CovenantsController → [Route("api/v1/covenants")].

- Error handling
  - AGENTS.md prescribes ProblemDetails. Code returns ProblemDetails with telemetry and logging (see CovenantsController).

- Database environment policy
  - AGENTS.md clarifies Local may use Access; non-Local must use SQL. Code enforces this policy in DataExtensions (StartupExtensions).

- Frontend structure and conventions
  - Co-located route components and hooks are allowed (review route has extensive co-location).
  - Service files live at frontend/src/services/api/<resource>.ts without “Service” suffix (e.g., covenants.ts).
  - Route-local types allowed via types.ts (e.g., app/admin/users/types.ts).
  - Next.js App Router and Tailwind present and configured.

Remaining discrepancies or risks
1) Frontend dependency violates “No React Query” rule
  - AGENTS.md → Data Fetching: “useEffect + native fetch. No axios, React Query, SWR, or RTK Query.”
  - Repository: frontend/package.json includes "@tanstack/react-query": "^5.29.0".
  - Action: Either remove @tanstack/react-query and refactor any usage to native fetch + custom hooks/services, or revise AGENTS.md to allow React Query. Since AGENTS.md says the rule is non-negotiable, the correct alignment is to remove the dependency and any usage.

2) Registration wording
  - AGENTS.md “Register I<Entity>Repository → Sql<Entity>Repository in Program.cs (Casrr.Api)”.
  - Code performs registrations inside StartupExtensions.AddDataProviders(), which is invoked from Program.cs.
  - Suggestion: Clarify wording to “Register in StartupExtensions.AddDataProviders (invoked from Program.cs)” to match the implementation and avoid confusion for future additions.

Recommended follow-up validations
- Verify there are no imports of '@tanstack/react-query' and no useQuery/useMutation in frontend/src. If present, refactor to native fetch in services/api/*.ts and feature hooks. Remove the dependency from package.json after refactor.
- Spot-check a couple of maintenance pages to ensure they:
  - Use a service + hook layer (no direct fetch in components).
  - Use ProblemDetails error handling conventions on the backend endpoints they call.
  - Use /api/v1/ in their service URLs.

Summary
- AGENTS.md has been updated to reflect the project accurately in all key architecture and policy areas (auth, data access, layering, API versioning, frontend structure, CI folders, database environment policy).
- The primary outstanding inconsistency is the presence of @tanstack/react-query in frontend/package.json despite the documented prohibition. Align by removing the dependency and any usage to fully comply with AGENTS.md.