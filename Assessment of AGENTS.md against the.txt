Re-assessment completed after your updates. The current UI-PATTERNS.md now closely mirrors the two maintenance pages.

Files reviewed
- UI-PATTERNS.md (updated)
- frontend/src/app/maintenance/covenants/page.tsx
- frontend/src/app/maintenance/cas-findings/page.tsx

1) What matches exactly

- Design palette and shared components
  - UI-PATTERNS.md (Section 1)
    - “slate” surfaces/text, “emerald” success
    - Use shared Button and Select
  - Code (both pages)
    - Imports: import Select from "@/components/ui/Select"; import { Button } from "@/components/ui/Button";
    - Toasts: className="rounded bg-emerald-600 text-white shadow px-3 py-2 text-sm"
    - Error banner: className="rounded border border-red-300 bg-red-50 text-red-800 p-3 text-sm"

- Page shell
  - Pattern (Section 2):
    <div className="p-4 space-y-4"> … <h1 className="text-xl font-semibold text-slate-800">…</h1>
  - Covenants:
    <div className="p-4 space-y-4"> … <h1 className="text-xl font-semibold text-slate-800">Covenants — Library Maintenance</h1>
  - CAS Findings:
    <div className="p-4 space-y-4"> … <h1 className="text-xl font-semibold text-slate-800">CAS Findings — Library Maintenance</h1>

- Toast system (array state, top-right, 3s auto-dismiss)
  - Pattern (Section 3):
    type ToastMsg = { id: number; text: string }; const [toasts, setToasts] = useState<ToastMsg[]>([]);
    setTimeout(() => remove after 3000)
    Render container: className="fixed right-4 top-4 z-50 space-y-2"
  - Code (both pages): identical type/shape, showToast with setTimeout, top-right render

- Error banner
  - Pattern (Section 4): pageError state name, non-dismissible, classes border-red-300 bg-red-50 text-red-800 p-3 text-sm
  - Code (both pages): identical state name + classes

- Filter bar
  - Pattern (Section 5): Label class "text-xs text-slate-600 mb-1", Select size="sm" className="bg-white min-w-[260px]"
  - Covenants:
    <label className="text-xs text-slate-600 mb-1">Filter by Category</label>
    <Select size="sm" className="bg-white min-w-[260px]" … />
  - CAS Findings: same with “Filter by Component”

- Add New form structure and behaviors
  - Pattern (Section 6): Card p-4, outline Button, grid layout md:grid-cols-6, single createError block
  - Covenants:
    <div className="bg-white border border-slate-200 rounded p-4">
    <div className="text-slate-700 font-semibold mb-2">Add New Covenant</div>
    <form className="grid grid-cols-1 md:grid-cols-6 gap-3 items-end" …>
    <Button type="submit" size="sm" variant="outline" disabled={isCreating}>{isCreating ? "Creating…" : "Create"}</Button>
    {createError && <div className="text-sm text-red-600 mt-2">{createError}</div>}
  - CAS Findings: identical structure with entity-specific fields

- Table wrapper, header bar, thead classes
  - Pattern (Section 7b):
    <div className="bg-white border border-slate-200 rounded overflow-hidden">
    <div className="px-3 py-2 bg-slate-50 text-slate-700 text-sm font-semibold border-b border-slate-200">
    <div className="overflow-auto">
    <table className="min-w-full text-sm">
    <thead className="bg-slate-50 text-slate-600">
  - Covenants:
    Exact match on these wrappers/classes
  - CAS Findings:
    Exact match

- Helper components placement and classes
  - Pattern (Section 7a): Define Th/Td at the bottom of the page file; Th px-3 py-2 text-left font-semibold border-b border-slate-200; Td px-3 py-2 align-middle
  - Covenants:
    Helpers are defined after the default export with identical classes
  - CAS Findings:
    Same

- SkeletonRows usage and shape
  - Pattern (Section 7c): SkeletonRows returns a div stack of animated bars; used via a single wrapper row:
    <tr><Td colSpan={N}><SkeletonRows /></Td></tr>
  - Covenants:
    {isLoadingRows ? (<tr><Td colSpan={5}><SkeletonRows /></Td></tr>) …}, SkeletonRows returns a div with 5 bars
  - CAS Findings:
    Same with colSpan={6}

- Row behavior: inputs always rendered; disabled toggles; PK plain text; Edit toggles to Cancel; Save/Delete always visible
  - Pattern (Section 8): Described exactly
  - Covenants:
    Disabled={!r.editing} on inputs/select; PK cell text; three buttons with Edit/Cancel text toggle
  - CAS Findings:
    Same

- Delete confirmation modal (a11y, focus trap)
  - Pattern (Section 9): role="dialog", aria-modal, aria-*, ESC/tab handling, backdrop "bg-black/40"
  - Covenants:
    useEffect focus trap; role/aria attrs; backdrop; restores body scroll; restores focus
  - CAS Findings:
    Same

- Color/state reference
  - Pattern (Section 10) enumerates:
    - Table scroll wrapper: overflow-auto; table: min-w-full
    - Row: border-t border-slate-200 align-top
    - PK cell: align-middle font-mono text-[12px] text-slate-700
    - Textarea (edit mode) class incl. shadow-sm and focus:ring-[color:var(--brand-primary)]/30
  - Code:
    All the above present in respective pages

2) What is different

- Toast container placement in JSX tree
  - Pattern (Section 2 and 3): “Toast Stack — always at bottom of JSX”
  - Covenants/CAS:
    Toast container is rendered near the top of the returned JSX (right after opening shell div), e.g.:
    <div aria-live="polite" className="fixed right-4 top-4 z-50 space-y-2">…</div>
  - Functional classes match; positional guidance differs.

- Create form input classes (height vs padding)
  - Pattern (Section 6 text input example):
    className="w-full border border-slate-300 rounded px-2 py-1 text-sm text-slate-700 focus:outline-none"
  - Covenants (create inputs):
    className="w-full h-8 border border-slate-300 rounded px-2 text-sm bg-white" (e.g., Code/Covenant Type/Order fields)
  - Differences:
    - Uses explicit h-8 instead of py-1
    - Adds bg-white
    - Omits text-slate-700 on create inputs

- Example vs actual column sets and index cell in sample row
  - Pattern (Section 7b) clarifies “No leading # index column — columns are entity-specific”
  - Pattern (Section 8b) example shows a “Row number” TD:
    <Td className="text-slate-400 text-xs">{index + 1}</Td>
  - Actual pages:
    Do not include an index column/cell in either table rows

3) What exists in the actual pages but is missing from UI-PATTERNS.md

- Toast container location near top of JSX
  - Covenants and CAS Findings render the toast container at the top of the return tree:
    <div aria-live="polite" className="fixed right-4 top-4 z-50 space-y-2">…</div>
  - Pattern instructs to place “at bottom of JSX” but does not document that rendering higher up is allowed; functionally identical due to fixed positioning. If pattern is meant to be prescriptive about placement for readability, this remains an undocumented variance.

- Create form inputs use h-8 and bg-white
  - Covenants:
    <input … className="w-full h-8 border border-slate-300 rounded px-2 text-sm bg-white" />
  - Pattern’s Section 6 example does not include h-8 or bg-white on the create form inputs. Section 10 does specify input classes generally but not h-8.

4) What is in UI-PATTERNS.md but not used anywhere in the actual code (in these two files)

- “Toast Stack — always at bottom of JSX”
  - Pattern editorial guidance says “always at bottom of JSX”
  - Both pages render the toast stack at the top of the JSX tree. Classes and behavior do match, just the placement differs.

- “Row number” TD in Section 8b example
  - Pattern snippet includes:
    <Td className="text-slate-400 text-xs">{index + 1}</Td>
  - Neither maintenance page renders a row-number column/cell.

- “self-end” on create button wrapper
  - Pattern (Section 6): <div className="md:col-span-1 self-end">…Button…</div>
  - Covenants: <div className="md:col-span-1">…Button…</div>
  - CAS Findings: <div className="md:col-span-1">…Button…</div>
  - The forms use grid items-end at the form level; self-end is not present in the code.

- TableRow component reference in Section 7b example
  - Pattern sample maps to <TableRow … />
  - Actual pages inline the <tr> markup; no separate TableRow component is used. The doc elsewhere (8b) also shows inline rows, so this is just an example artifact not reflected in these two files.

Concrete code vs pattern excerpts

- Toast container placement
  - Pattern:
    {/* Toast Stack — always at bottom of JSX, outside content flow */}
    <div aria-live="polite" className="fixed right-4 top-4 z-50 space-y-2">…</div>
  - Covenants (top of return):
    <div aria-live="polite" className="fixed right-4 top-4 z-50 space-y-2">…</div>

- Create form input height
  - Pattern (Section 6):
    className="w-full border border-slate-300 rounded px-2 py-1 text-sm text-slate-700 focus:outline-none"
  - Covenants (Code field):
    className="w-full h-8 border border-slate-300 rounded px-2 text-sm bg-white"

- SkeletonRows usage (now aligned)
  - Pattern (Section 7c):
    <tr><Td colSpan={N}><SkeletonRows /></Td></tr>, SkeletonRows returns div bars
  - Covenants:
    <tr><Td colSpan={5}><SkeletonRows /></Td></tr> and SkeletonRows returns the div with bars
  - CAS Findings:
    <tr><Td colSpan={6}><SkeletonRows /></Td></tr>

- Row PK cell size (aligned)
  - Pattern (Section 10):
    PK cell: align-middle font-mono text-[12px] text-slate-700
  - Covenants:
    <Td className="align-middle font-mono text-[12px] text-slate-700">{r.original.code}</Td>
  - CAS Findings:
    <Td className="align-middle font-mono text-[12px] text-slate-700">{r.original.component}</Td>
    <Td className="align-middle font-mono text-[12px] text-slate-700">{r.original.findingCode}</Td>

- Table scroll wrapper and width (aligned)
  - Pattern (Section 10):
    Table scroll wrapper: overflow-auto; Table: min-w-full
  - Covenants:
    <div className="overflow-auto"><table className="min-w-full text-sm">
  - CAS Findings:
    <div className="overflow-auto"><table className="min-w-full text-sm">

Conclusion
- UI-PATTERNS.md now matches the real implementation for the two maintenance pages across palette, shared components, page shell, toast system, error banner, filter bar, add form, table wrappers, helpers, skeleton loading, row behavior, modal confirmation, and most class tokens.
- Minor residual differences remain:
  - Toast container placement in the JSX tree (pattern says “bottom of JSX”; pages render it at top)
  - Create form inputs use h-8 and bg-white (pattern example uses py-1 and text-slate-700 without h-8)
  - Section 8b row example includes a “Row number” TD not used by either page
  - “self-end” on the Button wrapper in the pattern form example is not present in the pages
- If the document intends true 1:1 strictness for future codegen, either:
  - Adjust the pattern examples for the create form inputs (allow h-8/bg-white), remove the row-number TD from the row example, and relax or clarify the toast container placement note; or
  - Update the two pages to add self-end in the button wrapper and change create-input classes to match the exact py-1/text-slate-700 combination.
This assessment cites the exact code and classes present in the repo files vs the current UI-PATTERNS.md.