Reassessment result

Verdict: AGENTS.md is now aligned with the actual repository in all material areas. The previously reported mismatches (authentication model, API versioning, repository pattern, project structure, CI folders, frontend co-location rules, DI registration location) have been corrected to reflect the codebase. Only minor refinements are suggested below.

Confirmed alignments (evidence-based)
- Authentication and authorization
  - Azure AD JWT via Microsoft.Identity.Web is documented and matches Program/StartupExtensions.
  - Controllers use named policies (e.g., RequireActiveUser), consistent with existing controllers.
  - Swagger Bearer/Graph schemes are acknowledged.

- Data access and architecture
  - Repositories use raw ADO.NET (SqlClient), optional Dapper; EF DbContext exists but is not the default. Matches Infrastructure/SqlServer/* repositories.
  - Clean layering matches actual layout: interfaces at Casrr.Application root, contracts under Reviews/Contracts, repositories in Infrastructure/SqlServer, controllers in Api/Controllers.
  - DI registration called out in StartupExtensions.AddDataProviders(), matching how repositories are currently registered.

- API design
  - Versioned routes standardized to /api/v1/{resource}, matching controllers (e.g., CovenantsController).
  - ProblemDetails mandated for errors; aligns with controller implementations.

- Frontend structure and conventions
  - Next.js App Router structure with co-located components/hooks under app routes allowed; matches review route structure.
  - Services under frontend/src/services/api/<resource>.ts with resource-noun filenames (no “Service” suffix) matches current code.
  - Route-local types via types.ts allowed; shared types under src/models.

- Monorepo and CI
  - Root folders list includes azure-pipelines/ alongside pipelines/.
  - Project inventory now states 4 core projects + tests, matching the solution.

Minor differences and recommended refinements
1) Domain vs contract return types in controllers
  - Some controllers (e.g., CovenantsController) return domain types directly (e.g., CovenantLibraryItem) rather than explicit Application-layer contracts.
  - Suggestion: either
    - Permit returning domain models for simple data shapes and document this exception, or
    - Require contracts for new endpoints and add a note to migrate older endpoints over time.

2) Contract naming convention
  - The document suggests a “Contract” suffix in examples (e.g., LoanCodeContract.cs). Actual files in Application commonly use naming like ReviewFormSaveModels.cs.
  - Suggestion: relax the suffix requirement or note both patterns as acceptable to avoid churn.

3) React Query presence as legacy
  - package.json contains @tanstack/react-query. The document now explicitly allows it to remain but forbids new usage and prescribes native fetch + useEffect through the service + hook layer.
  - Optional hardening: add a lint rule/code scan step to fail on new imports of '@tanstack/react-query' to enforce the rule.

Everything else reads consistent with the repository and current engineering practices in this codebase.

Conclusion
AGENTS.md now accurately reflects the project’s actual architecture, technologies, and conventions. No blocking discrepancies remain. Consider the minor refinements above to reduce ambiguity around controller return types and contract naming; otherwise, the file is suitable to act as the project’s non-negotiable constitution.